/**
 * Published npm CLI package — use everywhere we show install/copy instructions.
 */
export const TERMASSIST_NPM_PACKAGE = "@manoj-ruler/termassist";

export const TERMASSIST_INSTALL_COMMAND = `npm install -g ${TERMASSIST_NPM_PACKAGE}`;
