% Reliability-Aware Hybrid Retrieval for Natural-Language-to-Shell-Command Assistance
% ACL/EACL Student Research Workshop, long paper -- ANONYMIZED REVIEW VERSION.
%
% This is a thin wrapper: the actual paper body lives in content.tex, shared identically with
% main.tex (the final/camera-ready version). Edit content.tex for any content change -- editing
% only one of the two wrappers would silently desync the submission versions.
%
% [review] mode auto-replaces whatever \author{} contains with "Anonymous ACL submission" in the
% rendered PDF (confirmed by reading acl.sty's own anonymize logic) and adds line numbers, so the
% placeholder \author block below is never actually shown.
%
% NOT COVERED by acl.sty's anonymization: this paper studies TermAssist, a real, named, public npm
% package. If the target venue's double-blind policy is strict, referencing the system by its real
% name in the BODY TEXT (content.tex) may itself be de-anonymizing regardless of anything this
% wrapper does. Unresolved, and the author's call -- see the note at the end of this file.

\documentclass[11pt]{article}
\usepackage[review]{acl}
\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}

\title{Reliability-Aware Hybrid Retrieval for Natural-Language-to-Shell-Command Assistance: \\ A Non-LLM Study}

\author{
  Anonymous ACL submission \\
  \small (author block hidden by acl.sty in review mode)
}

\begin{document}
\maketitle

\input{content}

\end{document}

% ============================================================================
% NOTE (not part of the paper -- delete before submitting)
% ============================================================================
% This is the ANONYMIZED REVIEW build: author identity hidden, line numbers on. Use main.tex
% instead for a camera-ready/non-blind copy.
%
% ANONYMIZATION RISK ACL.STY CANNOT COVER: TermAssist is a real, named, public npm package. Global
% npm metadata ties that package to a specific, identifiable public account. If the target venue's
% double-blind policy is strict, referencing the system by its real name in content.tex may itself
% be de-anonymizing regardless of anything acl.sty does. Unresolved -- the author's call: (a)
% accept the risk (many venues explicitly permit referencing one's own public prior artifact), (b)
% check the specific target venue's policy, or (c) use a pseudonym for the system name in this
% review version and reveal the real name only in main.tex (would require content.tex to no longer
% be perfectly shared between the two -- a bigger structural change, not done here).
%
% Page count: 9 pages total as of the last build (7 pages body text, under the ACL/EACL SRW 8-page
% limit; References on pages 8-9). Recheck after any edit to content.tex, figures, or tables.
