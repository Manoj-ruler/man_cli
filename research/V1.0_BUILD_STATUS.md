# TermAssist Research v1.0 — Build Status & Acceptance-Gate Tracker

Execution record for the autonomous build against `TERMASSIST_RESEARCH_V1.0_SPEC.md`. Records what
was built, what was verified, and — honestly — what is blocked on human input and therefore NOT
auto-executed. Updated as phases complete.

## Phases executed (autonomous, this cycle)

| Phase | What | Verification | Commit |
|---|---|---|---|
| P1 | Delete false FAISS comment (`cli/index.js`) | Baseline 0/150 mismatch after edit; comment-only diff | `663d02d` |
| P2 | Bootstrap CIs + Holm correction on existing v0.1/v0.2 | Internally consistent w/ McNemar; calibration survives Holm on both | `7676429` |
| P3 | Split B intent-held-out (GroupKFold) generalization on v0.2 | 171 groups, 0 split (invariant asserted); near-zero acc/OOD gap | `1a0479c` |
| P4 | Additive corpus schema migration + validator + vocab + manifest | Baseline 0/150 after migration; validator 0 errors | `f9e7ca7` |
| P5 | Preregistration + this status tracker | (docs) | this commit |

## Key results produced this cycle

- **Statistical hardening (spec §8.2 — was missing):** after Holm correction, **calibration ECE
  reduction is significant on BOTH benchmarks** (Holm p=0.0004 / 0.0003) and **OOD detection on
  v0.2** (0.00006); **BM25→hybrid accuracy barely survives on v0.1 (0.047), fails on v0.2 (0.070)**.
  Bootstrap CIs added for all deltas + ECE. → `research/results/stats/`.
- **Intent-held-out generalization (spec §5.2 — was missing):** accuracy 79.2% vs 79.3%, OOD AUROC
  0.898 vs 0.901 — **reliability findings generalize to unseen intents**; calibration attenuates
  (ECE→0.101 vs →0.074), disclosed. → `research/results/v0.2/SPLIT_B_NOTES.md`.
- **Corpus schema (spec §1):** migrated to the 9-field additive schema, backward-compatible,
  validator + controlled category vocabulary in place. Headline size correctly reported as **279
  win32-visible unique intents.**
- **Integrity:** false FAISS comment removed (gate E-2).

## Acceptance-gate status (spec §11)

| Gate | Status | Note |
|---|---|---|
| A-1 baseline reproduces after schema change | **PASS** | 0/150, twice (after comment fix + after migration) |
| A-2 corpus validator passes §1.4 | **PASS** | 0 errors, 0 exact-dup commands |
| A-3 corpus 500–800 human_validated intents | **BLOCKED-ON-HUMAN** | 279 intents, 0 human_validated — expansion needs human authoring/validation; bulk LLM prohibited |
| A-4 corpus manifest + tag | **PARTIAL** | migration manifest written (pre-v1.0); `v1.0-corpus` tag deferred until A-3 |
| B-1 AI-query revalidation, κ≥0.7 | **BLOCKED-ON-HUMAN** | requires a genuine independent second human annotator; cannot be self-annotated |
| B-2 canonical controls flagged | **SPEC'D, not applied** | applies when v1.0 benchmark is built; schema field `is_control` defined |
| B-3 OOD/ambiguity gates | **SPEC'D** | empirical gate defined; applied at v1.0 build |
| B-4 v1.0 benchmark frozen | **BLOCKED-ON-HUMAN** | depends on B-1 |
| C-1 split registry, Split B no-leakage | **PASS (v0.2)** | `splits-b-registry.json`, 0 groups split |
| C-2 all tuned params dev-fold-only | **PASS** | Split A + Split B both nested; preregistered |
| D-1 experiments on v0.1+v1.0, Split A+B | **PARTIAL** | v0.1/v0.2 done incl. Split B; v1.0 pending B-4 |
| D-2 Holm-adjusted p-values | **PASS** | `holm-correction-results.json` |
| D-3 bootstrap CIs | **PASS** | `bootstrap-ci-results.json` |
| D-4 preregistration predates finals | **PASS** | `research/results/v1.0/PREREGISTRATION.md` (this cycle) |
| D-5 independent safety set | **BLOCKED-ON-HUMAN** | needs human labels independent of the classifier |
| D-6 functional eval ≥40 sandboxed | **BLOCKED (scope)** | only ~15 queries are safely/deterministically sandboxable without a placeholder-substitution system; expansion needs new human-authored safe tasks |
| E-1 unified clean-room runner | **PARTIAL** | v0.1 `run_all.js` exists; v1.0 runner deferred until v1.0 inputs exist |
| E-2 false FAISS comment removed | **PASS** | `663d02d` |
| E-3 no fabricated numbers | **PASS** | every number traces to a committed result file |
| F-1 claims match audit "can-make" list | **N/A this cycle** | manuscript update is a later phase |
| F-2 accuracy = replication finding | **PASS** | manuscript already reframed; stats now reinforce it |

## What is BLOCKED-ON-HUMAN (deliberately NOT auto-executed)

These require genuine human input; auto-executing them would mean either bulk-LLM fabrication
(prohibited by spec §12) or me impersonating an independent human annotator (dishonest). They are
scaffolded (schema, gates, validators exist) and left for a human:

1. **Corpus expansion 279 → 500–800 human-validated intents** (spec §2). Needs human authoring
   from man/tldr/official docs + human validation.
2. **Independent second-annotator re-validation of AI-authored benchmark queries, κ≥0.7** (§4.4).
3. **Independent human-labeled safety set (~50 items)** (§7.2).
4. **Functional-eval expansion to ~40–60 safe sandboxed tasks** (§15) — needs new human-authored,
   placeholder-free, host-safe tasks.

## Next autonomously-executable work (if directed)

- Unify P2/P3 into a `run_stats_and_generalization.js` orchestrator (low value; standalone works).
- Manuscript update to fold in the Holm-corrected stats + Split B generalization column.
- Split B on v0.1 for symmetry (currently v0.2 only).
Everything else on the v1.0 critical path is gated on the four human tasks above.
