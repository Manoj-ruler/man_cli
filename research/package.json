{
  "name": "termassist-research",
  "version": "0.1.0",
  "private": true,
  "description": "Research-only dependencies for TermAssist dense/hybrid retrieval experiments. Isolated from cli/ and the web app so the shipped product never gains an embedding/LLM dependency.",
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
